# Governance

Stop the workstream if batch behavior would:

- bypass the verified SPEC-004 single-file decoder;
- follow symlinks or permit source/output/root escape;
- overwrite REC sources or unrelated files;
- place inline secrets in manifests or summaries;
- publish partial summaries as complete;
- broaden category or device claims without protected evidence.

Resume only after batch decoding is an approved product priority.
